<?xml version="1.0" encoding="utf-8"?>
<host-apdu-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/servicedesc"
    android:requireDeviceScreenOn="false"
    android:requireDeviceUnlock="false">
    <!-- Standard NFC Forum Type-4 NDEF application AID — using the
         well-known AID (not an app-specific one) is what makes any
         stock NFC reader on the other phone recognize this as a
         readable NDEF tag, no Rota app needed on their end. -->
    <aid-group android:description="@string/aiddescription" android:category="other">
        <aid-filter android:name="D2760000850101" />
    </aid-group>
</host-apdu-service>
