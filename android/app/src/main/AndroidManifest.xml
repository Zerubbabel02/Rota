<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- App Links: when a tapped/scanned claim link is opened on a
                 device that also has Rota installed, Android routes it
                 straight into the app instead of a browser tab (verified
                 via the assetlinks.json already hosted for WebAuthn). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="rota-app-zerubbabel1.vercel.app" />
            </intent-filter>

            <!-- Google sign-in's return trip from the system browser — a
                 custom scheme, not another App Link, because App Links only
                 intercept the *initial* external navigation to them; the
                 same-tab redirect chain Google/Supabase's OAuth callback
                 goes through never re-triggers that. See NATIVE_OAUTH_REDIRECT_URL
                 in App.jsx. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.rota.app" android:host="auth-callback" />
            </intent-filter>

        </activity>

        <!-- Accept/Decline buttons on the quick-accept notification — a
             BroadcastReceiver so they work without the app running. -->
        <receiver
            android:name=".RotaTapActionReceiver"
            android:exported="false" />

        <!-- Replaces (not adds to) @capacitor/push-notifications' own
             MessagingService — RotaMessagingService extends it, so every
             existing push still behaves the same, except tap_quick_accept
             data messages, which it shows as a native Accept/Decline banner
             instead of relying on OS-level tag dispatch to a closed app
             (that path proved unreliable across devices — see
             RotaTapProtocol.kt's history). -->
        <service
            android:name="com.capacitorjs.plugins.pushnotifications.MessagingService"
            tools:node="remove" />
        <service
            android:name=".RotaMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- FCM notifications default to a generic bell icon and system
             accent color unless told otherwise — this uses our own mark
             and brand color instead. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_rota" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/ic_launcher_background" />

        <!-- Emulates a standard NFC Type-4 NDEF tag while active, so any
             phone tapping ours (no app required on their end) gets the
             normal "open this link?" system prompt, same as tapping a
             physical NFC sticker. -->
        <service
            android:name="com.thanmgan22.plugins.nfchce.KHostApduService"
            android:exported="true"
            android:enabled="true"
            android:permission="android.permission.BIND_NFC_SERVICE">
            <intent-filter>
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.cardemulation.host_apdu_service"
                android:resource="@xml/apduservice" />
        </service>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />
    <uses-feature android:name="android.hardware.nfc.hce" android:required="false" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Required for the Airtime/Data contact picker (@capacitor-community/contacts).
         The plugin groups READ_CONTACTS + WRITE_CONTACTS under one runtime
         permission alias, so both must be declared here or the OS silently
         denies the whole alias with no dialog, even though only reading a
         picked contact is ever actually used. -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <!-- Lets "open app" mode actually open the app automatically (same
         mechanism an incoming call uses), instead of only showing a
         notification the receiver has to tap first. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
</manifest>
